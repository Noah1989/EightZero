Typing in machine code by hand is boring and error-prone ;-)

This is a simple script that can load code into the machine
either through the ZDI debugger or the loader program.

It depends on pexpect and talks to the target through a tty
such as /dev/ttyUSB0
